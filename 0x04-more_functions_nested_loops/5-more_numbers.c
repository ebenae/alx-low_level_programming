#include "main.h"
#include <stdio.h>

/**
 * Print numbers between 0 to 14, 10 times.
 */
void more_numbers(void)
{
	int i;
	int x;

	for (x = 1; x <= 10; x++)
	{
		 for (i=0; i<=14; i++){
            _putchar(i);
         }
		}
	_putchar('\n');
	}

